[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "echo-melody-harmonizer"
version = "0.1.0"
description = "Genetic-algorithm melody harmonization tools."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
